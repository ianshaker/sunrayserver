// ============================================================================
// Расписание AI — настройки.
// ============================================================================

// Только schedule-ai. НЕ трогаем CALL_AI_* / DAILY_HIGHLIGHTS_* / другие отделы.
// Было через call-ai SUMMARY (= gemini-2.5-flash @ us-central1).
//   SCHEDULE_AI_GEMINI_MODEL / SCHEDULE_AI_VERTEX_LOCATION
module.exports = {
  GEMINI_MODEL: process.env.SCHEDULE_AI_GEMINI_MODEL || "gemini-2.5-flash",
  VERTEX_LOCATION: process.env.SCHEDULE_AI_VERTEX_LOCATION || "us-central1",

  // Сколько дней от «сегодня» считаем разумным диапазоном для даты расписания
  // (защита от кривого разбора относительных дат моделью — не ограничение бизнес-логики).
  MAX_DATE_RANGE_DAYS: 365,

  // Короткий вердикт сверху (этап 3.5). Менеджер видит вердикт ВСЕГДА:
  // если LLM выключена/недоступна/её текст не прошёл проверку на соответствие
  // найденным данным — render.js подставляет детерминированный fallback-вердикт
  // (без LLM). Этот флаг только выключает попытку получить более человечную
  // формулировку от Gemini, не сам вердикт.
  ENABLE_COMMENTARY: true,

  // Сколько ближайших совпадений (мастер+дата) показывать для "nearest_city"
  // ("Бог любит троицу" — договорённость по бизнесу).
  MAX_NEAREST_GROUPS: 3,

  // Если в искомом городе совпадений нет — ищем в соседних городах по прямому
  // расстоянию (Хаверсин, см. cityCoordinates.js), но только в пределах этого
  // радиуса. Иначе «похожий город» может оказаться за 150 км — это уже не
  // похоже, а вредная подсказка. 40 км — разумный радиус одной рабочей зоны
  // выездного мастера по Подмосковью.
  MAX_NEARBY_RADIUS_KM: 40,
};
